# ═══════════════════════════════════════════════════════════════
# PSYCHO PORTAL — Environment Configuration
# Copy this file to .env and fill in your values
# ═══════════════════════════════════════════════════════════════

# ── LLM Provider ────────────────────────────────────────────────
# Options: "anthropic" | "ollama"
LLM_PROVIDER=anthropic

# ── Anthropic (dev default) ──────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-your-key-here
# Cheapest: claude-haiku-4-5-20251001 | Better: claude-sonnet-4-6 | Best: claude-opus-4-6
ANTHROPIC_MODEL=claude-haiku-4-5-20251001

# ── Ollama (local, no API key needed) ────────────────────────────
# Uncomment and set LLM_PROVIDER=ollama to use local models
# OLLAMA_MODEL=llama3.2
# OLLAMA_BASE_URL=http://localhost:11434

# ── Storage Paths ────────────────────────────────────────────────
# All relative to project root. Defaults shown below.
# DATA_DIR=data
# DB_PATH=data/psycho.db
# GRAPH_PATH=data/graph
# VECTOR_PATH=data/vectors
# JOURNAL_PATH=data/journals

# ── Agent Behavior ───────────────────────────────────────────────
# MAX_SHORT_TERM_MESSAGES=20
# MAX_CONTEXT_MEMORIES=5
# EXTRACTION_ENABLED=true
# REFLECTION_ENABLED=true

# ── Personality Defaults (0.0-1.0 = 0%-100%) ─────────────────────
# These are the initial values. Adjustable at runtime via:
#   - Web UI personality panel (⚙ Personality button)
#   - Chat: "set humor to 90%", "be more direct", "dial down the sass"
#   - PATCH /api/personality
#
# Defaults are calibrated for Jarvis + TARS style:
# PERSONALITY_HUMOR=0.75        # 75% = dry wit, perfectly timed
# PERSONALITY_WIT=0.82          # 82% = sharp, layered observations
# PERSONALITY_DIRECTNESS=0.88   # 88% = leads with the answer, no padding
# PERSONALITY_WARMTH=0.72       # 72% = genuine care, not sappy
# PERSONALITY_SASS=0.38         # 38% = light ribbing when warranted
# PERSONALITY_FORMALITY=0.12    # 12% = very casual, uses contractions
# PERSONALITY_PROACTIVE=0.82    # 82% = volunteers observations, anticipates needs
# PERSONALITY_EMPATHY=0.78      # 78% = notices mood, adapts tone

# ── Proactive System ─────────────────────────────────────────────
# PROACTIVE_ENABLED=true        # Enable reminders, check-ins, calendar alerts
# CHECKIN_ENABLED=true          # Enable morning/evening check-ins

# ── API Server ───────────────────────────────────────────────────
# API_HOST=0.0.0.0
# API_PORT=8000

# ── TTS (Text-to-Speech for Voice Mode) ─────────────────────────
# Options: "browser" (robotic, Web Speech API) | "local" (Kokoro) | "openai" | "elevenlabs"
#
# RECOMMENDED: Kokoro — open-source, 82M params, sounds remarkably human
#   Install: pip install kokoro soundfile
#   First launch auto-downloads ~450MB model from HuggingFace (cached permanently)
TTS_PROVIDER=local
LOCAL_TTS_BACKEND=kokoro
#
# Kokoro voices (pick one):
#   British male   (JARVIS-like) : bm_george, bm_lewis, bm_daniel, bm_fable
#   American male                : am_adam, am_michael, am_echo, am_liam
#   American female (warm)       : af_heart, af_bella, af_nicole, af_sky
#   British female               : bf_emma, bf_alice, bf_isabella, bf_lily
LOCAL_TTS_VOICE=bm_george
LOCAL_TTS_SPEED=1.0

# ── Browser TTS (fallback, robotic) ─────────────────────────────
# TTS_PROVIDER=browser

# ── OpenAI TTS — high quality, ~$0.015/1k chars ─────────────────
# TTS_PROVIDER=openai
# OPENAI_API_KEY=sk-your-openai-key-here
# TTS_VOICE=alloy    # alloy | echo | fable | onyx | nova | shimmer

# ── ElevenLabs TTS — highest quality, realistic voices ──────────
# TTS_PROVIDER=elevenlabs
# ELEVENLABS_API_KEY=your-elevenlabs-key-here
# ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM  # default: Rachel

# ── STT (Speech-to-Text for Voice Mode) ─────────────────────────
# Options: "browser" (free, Web Speech API) | "whisper_local"
# STT_PROVIDER=browser

# Local Whisper STT — fully offline (install: pip install faster-whisper)
# STT_PROVIDER=whisper_local
# WHISPER_MODEL=base            # tiny | base | small | medium | large-v3
# WHISPER_DEVICE=cpu            # cpu | cuda | auto
# WHISPER_COMPUTE_TYPE=int8     # int8 | float16 | float32
# WHISPER_LANGUAGE=             # Leave blank for auto-detect
# WHISPER_BACKEND=faster_whisper # faster_whisper | openai_whisper

# ── Web Search ──────────────────────────────────────────────────
# Auto-injects live search results when query needs current data
# WEB_SEARCH_ENABLED=true

# Optional: Brave Search API key for better results (free tier: 2000 req/month)
# Sign up at https://api.search.brave.com
# BRAVE_API_KEY=your-brave-api-key-here

# ── Google Calendar Integration (optional) ───────────────────────
# Set path to your Google OAuth2 credentials JSON file
# Download from: console.cloud.google.com → APIs & Services → Credentials
# GOOGLE_CALENDAR_CREDENTIALS=data/google_credentials.json
